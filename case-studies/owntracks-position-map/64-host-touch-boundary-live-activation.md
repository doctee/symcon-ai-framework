# Host Touch Boundary Live Activation

**Status:** Exact package-only activation complete; independent structural and
security postflight passed; physical iPhone acceptance pending

**Date:** 2026-09-02

## Approved Scope

The gate authorized repository correction, browser verification and live
activation through postflight. It did not authorize a configuration change,
repository commit, publication, cache purge, retained-artifact cleanup,
OwnTracks-object change, archive/logging change, visualization-link change,
WebHook change or provider-budget change.

## Transfer And Activation

A fresh Symcon-MCP-only preflight verified healthy status, exact preceding
package and configuration identities, unchanged provider policy and free
transaction paths. The 195,458-byte archive was transferred in six private
bounded chunks. Windows verified its SHA-256 digest, exactly 36 safe regular
entries, all 34 payload sizes and digests, package identity and the concrete
46-pixel renderer contract before staging.

The transaction retained a byte-exact private configuration backup, moved the
preceding package to an identity-qualified rollback boundary, activated the
verified stage and performed one targeted Module Control reload. No property
was written and no configuration `ApplyChanges()` transaction was performed.
Automatic rollback was armed but not required.

The active identity is
`6cfc1cba63add51505a460f139ea5c547dd0ccd88c7ac1a2e18c6a1cac800bb0`.
The immediately preceding identity
`7a9cc8562606eb08357553841590cb3665557476e8420b53c11df64b575c4b71`
remains the complete rollback package.

## Independent Postflight

A separate read-only probe verified:

- healthy status `102` and exact active and rollback package identities;
- the active renderer contains the six-pixel frame, 46-pixel interactive
  boundary and upper non-interactive status placement;
- complete configuration, provider envelope and WebHook Control remained
  byte-identical;
- gateway and provider request, concurrency and viewport budgets remained
  unchanged;
- three source contracts retained type triples `[3, 1, 1]` and all nine
  source variables remained logged;
- two visualization links remained unchanged;
- no stage or failed-candidate residue remained; and
- private upload, configuration backup and rollback package were retained.

The provider cache remained intact with 51 regular files, 1,710,062 bytes and
no symbolic links. Neither activation nor postflight opened the map or caused a
deliberate provider request.

## Privacy And Remaining Acceptance

No ObjectID, source key, tracker identifier, coordinate, tile index, movement
history, private path, Connect origin or host metadata is recorded here.

Physical acceptance should confirm on iPhone that `View`, `Source` and `Day`
react immediately, that the position count appears above the labels, and that
the frame may visually extend into the host band without intercepting the
fields. Commit, publication, cache purge and retained-artifact cleanup remain
closed.
