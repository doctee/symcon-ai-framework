# Provider Fallback Live Activation

**Status:** Exact-package and private tile-fallback activation complete;
independent structural and security postflight passed; physical acceptance
pending

**Date:** 2026-09-01

## Authorized Scope

The gate authorized private transfer and activation of the reviewed 36-file
package plus a configuration delta limited to
`ProviderConfiguration.tileFallback`. It did not authorize an OwnTracks,
archive, logging, static tile-set, WebHook Control, visualization-link,
publication, commit or retained-artifact cleanup change.

The fallback remains subordinate to the existing immutable private XYZ
authority. A provider request is possible only for an authenticated,
server-recorded current selection after a static miss and all spatial, request
and byte budgets accept it.

## Fresh Preflight And Transfer

The Symcon-MCP-only preflight confirmed one healthy pilot instance, three
consistent source contracts, active logging, two unchanged visualization
links, the Connect-protected WebHook boundary, a canonical private static tile
authority through zoom 14 and disabled provider fallback.

The new package identity was
`ebc2ee0b681c68f32923f7fa4565e954b0ff7bbbc07843e5ba44ca0f21ba6249`.
The first local transfer archive contained five harmless directory entries in
addition to the expected files. The activation contract rejected that shape
before extraction. The exact inactive upload was hash-gated, replaced and
retransmitted as a 192,987-byte archive containing exactly 36 regular files.
Windows independently verified the archive SHA-256, path safety, entry count,
provenance map and every file size and digest before creating the inactive
stage.

No private contact value, Referer origin, ObjectID, label, coordinate, tracker
identifier or movement record was written to repository evidence.

## First Activation And Automatic Rollback

Immediately before mutation, the transaction rechecked the active and staged
package identities, exact complete configuration, provider policy, WebHook
Control configuration, both links, verified upload, rollback path and an
exclusive byte-exact private configuration backup.

The first package switch stopped at the targeted Module Control reload because
the call used the library display name. This installation requires the module
directory name as the reload identifier. The candidate was quarantined and the
preceding package was restored. The post-rollback probe independently confirmed
the previous package identity, byte-identical configuration and healthy status.
The tile-fallback property had not been written.

## Corrected Retry

A separate read-only retry preflight verified the restored package, exact
quarantined candidate, upload, configuration backup, free transaction paths
and the installation-specific reload contract. The candidate was restaged,
atomically activated and reloaded using the module directory identifier.

Only then was `ProviderConfiguration.tileFallback` changed. The bounded live
policy is:

- OSM Standard raster only on an eligible static miss;
- maximum zoom 14, matching the browser and static authority;
- one-tile viewport ring and at most 128 spatially allowlisted tiles;
- at most two concurrent and 30 upstream requests per minute;
- at most eight requests and 4 MiB of successful image bytes per selection;
- two-second per-request timeout and 60-second negative result lifetime; and
- identifiable application User-Agent plus the separately approved private
  Connect Referer origin.

The contact and Referer values remain private live configuration and are not
present in this document or other public artifacts.

## Independent Postflight

The independent read-only postflight confirmed:

- healthy instance status 102;
- exact active 36-file identity
  `ebc2ee0b681c68f32923f7fa4565e954b0ff7bbbc07843e5ba44ca0f21ba6249`;
- preceding identity
  `29c274c8433826848bb1c20f91fffb4e1b4852262b83e3c1bfb292c7d1aeb697`
  retained as the complete rollback package;
- current configuration identity
  `93247c1e735e7415f0c57829db1b71750107c35a59bc928c4c87c34458e24b1e`;
- provider identity
  `4850496c33e6e172be6938bdcd15806ac9ea0c3fd6f4be95b22eb3bd51c55d63`;
- byte-exact rollback configuration retained privately;
- an exact configuration delta limited to the tile-fallback member;
- three unchanged source contracts and logging states;
- canonical, unchanged static tile authority;
- unchanged ephemeral-header Connect protection and WebHook Control
  configuration;
- two unchanged visualization links;
- no staging or failed-candidate residue; and
- verified upload and rollback artifacts retained.

The cache boundary contained only two small metadata/lock files totaling 612
bytes. There was no provider tile-content file, so the structural activation
postflight inferred no live map-tile request. No browser was opened by this gate
and no private OwnTracks tile index was deliberately transmitted for physical
testing.

## Rollback And Remaining Gate

Rollback restores the retained previous package, reloads the exact module
directory and writes the retained complete previous configuration. That action
is not needed while the current postflight remains healthy and requires a new
gate unless invoked automatically by the activation transaction itself.

Physical Safari/iPad/iPhone acceptance is still required. Opening the live map
may cause eligible missing XYZ tiles for the selected OwnTracks view to be sent
to OSMF under the approved policy. Publication, commit, cache purge, upload or
rollback cleanup and changes to the static tile revision remain separately
closed.
