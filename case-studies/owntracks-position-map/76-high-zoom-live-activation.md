# High-Zoom Live Activation

**Status:** Package, configuration and private static revision active;
provider-reaching browser acceptance complete

**Date:** 2026-09-03

## Controlled Transfer

The 88,474,062-byte extension archive was transferred through a temporary
LAN-only endpoint. The endpoint accepted one exact host, path, source address,
one-time capability and mandatory byte ranges of at most eight MiB. All eleven
blocks passed exact 206, Content-Range, content-length and destination offset
checks. Windows independently verified the complete archive SHA-256 before
extraction.

Every ZIP entry was required to be unique, relative, regular, unencrypted and
inside the exact tile, module or manifest roots. Extraction used explicit
per-entry streams into fresh directories. The temporary server and capability
were removed immediately after transfer verification; the Windows upload and
all transaction residues were removed after activation.

## Immutable Revision

The preceding z0-14 authority was verified independently before its files were
reused as content-preserving NTFS hard links. The new locally rendered z15-18
files were then combined with it in a new immutable revision. The old revision
was neither removed nor reconfigured and remains the tile rollback boundary.

Windows reproduced the exact complete inventory:

- 33,721 regular PNG files;
- 768,095,351 bytes;
- maximum tile size 98,363 bytes; and
- inventory SHA-256
  `55e7e2384e62b92a4789b5be4783f26fabcf15b43016913fdd1ad642ad38c616`.

## Activation

The first transaction stopped on byte-level configuration readback because
IP-Symcon normalizes serialized JSON. It restored the preceding module and
byte-exact configuration in the same execution.

The retry retained all security gates and compared the complete decoded
configuration canonically. It accepted only the authorized semantic delta:

- basemap, static authority and provider fallback maximum zoom 18;
- the new immutable tile revision;
- 48 visible provider requests per selection; and
- 60 provider requests per minute.

All other provider, capability, source, ETA, archive, logging, visualization
and WebHook properties remained semantically identical. The active 36-file
module identity is
`82c148a5f1e7789db1850641f5ea45f44322f8f4e8b2c84ea354b53982d2de9c`.
The immediate complete rollback identity is
`9a00040d6f4491cc1f330365c20d06d01abd0c8d0da179b1f1632e74e56dd97c`.

## Independent Postflight

A separate Symcon-MCP readback confirmed:

- healthy instance status 102;
- every payload hash in the active and immediate rollback package;
- the complete new tile inventory;
- the complete authorized configuration delta; and
- a uniform unauthenticated nine-byte 404 with no-store and nosniff.

The provider-free synthetic browser test proved zoom 18 and the two-retry
ceiling. The separately confirmed in-app live browser acceptance then used the
configured OSM Standard provider for the visible, position-derived XYZ tile
indices only.

The calm final checks confirmed:

- a complete current-position background at zoom 18, with all tiles needed by
  the final view succeeding and no remaining visible miss;
- a complete fit view for a representative long-distance historic path at
  zoom 6, with no new failed request in the final selection;
- fail-closed rejection of a stale capability during a deliberately fast
  multi-control change, followed by recovery on one normal selection action;
- bounded behavior during an artificial twelve-step rapid-zoom stress run;
  this run intentionally exhausted the per-selection/minute budgets and is not
  treated as an ordinary interaction result; and
- at the iPad-sized check, no horizontal or vertical document overflow and no
  fitted position underneath a control overlay.

The browser viewport was reset and the temporary live-acceptance tab was closed
after the checks.

No repository commit, provider publication, cache purge, existing OwnTracks
object change, archive change, logging change or visualization-link change was
performed.
