# Control, Tooltip And ETA Live Activation

**Status:** Exact-package activation and independent structural postflight
complete; physical acceptance pending

**Date:** 2026-09-01

## Authorized Scope

The gate authorized private transfer and exact package-only activation of the
repository-accepted control, tooltip and explanatory ETA correction. It did not
authorize configuration, provider, tile-set, archive, logging, commit,
publication or retained-artifact cleanup changes.

## Transfer And First Automatic Rollback

The fresh read-only preflight confirmed the exact preceding 29-file package,
unchanged configuration and provider hashes, all three source roles and logging
states, unchanged WebHook protection and visualization links, a default-deny
same-origin content-security policy and free identity-qualified transaction
paths.

The 29-file, 172376-byte transfer archive was verified locally, after bounded
chunk transfer and before path-safe staging. The first activation reached the
candidate postcheck but rejected three verification strings that source
minification and CSS formatting do not preserve literally. The transaction
immediately quarantined the candidate, restored the preceding exact package and
performed a healthy targeted reload. Configuration and live objects remained
unchanged.

## Verified Retry

A separate read-only retry preflight proved the restored active package, exact
quarantined candidate, retained upload and byte-exact configuration backup. It
also verified the field-bound CSS and stale-ETA presentation directly in the
candidate files. Only then was the candidate restaged and atomically activated.

The corrected postcheck used the deterministic package identity plus stable,
user-visible CSS and ETA evidence. No property was written and no
`ApplyChanges` operation was issued. The retry completed without invoking its
automatic rollback path.

## Independent Postflight

An independent read-only postflight confirmed:

- active exact package identity
  `29c274c8433826848bb1c20f91fffb4e1b4852262b83e3c1bfb292c7d1aeb697`;
- exact preceding package identity
  `8561019a0b3946e638f88cb5369a119da7702e839df5e84dea7ac5bafa207230`
  retained for rollback;
- byte-identical module and provider configuration;
- unchanged source roles, datatypes and logging states;
- unchanged WebHook configuration, no persistent candidate hook and both
  unchanged visualization links;
- exactly three browser sources and no external-path source;
- bounded field CSS and explanatory stale-ETA presentation in the live tile;
- unchanged default-deny, same-origin content-security policy with no external
  tile host; and
- no staging or failed-candidate residue.

The verified upload, configuration backup and rollback package remain retained.
No ObjectID, label, tracker identifier, coordinate or movement history was
written into repository evidence.

## Remaining Gates

Physical Safari/iPad/iPhone acceptance remains required. Commit, publication,
provider or tile-set changes and cleanup of retained artifacts remain separately
closed.
