# Viewport Generation Live Activation

**Status:** Exact package-only activation complete; independent structural,
security and historical-archive postflight passed; physical tile acceptance
pending

**Date:** 2026-09-02

## Approved Scope

The gate authorized repository correction, browser verification and private
live activation through postflight. It did not authorize a configuration
change, repository commit, publication, cache purge, OwnTracks-object change,
archive/logging change, visualization-link change, WebHook change or provider
budget change.

## Transfer And Activation

A fresh Symcon-MCP-only preflight verified one healthy owner, the exact active
package and complete configuration, unchanged provider and WebHook boundaries,
and a collision-free transaction. The 198,165-byte archive was transferred in
seven bounded chunks. Windows verified its archive digest, exactly 36 safe
regular entries, all 34 provenance payloads, every payload size and digest,
the fileset identity and the new viewport and historical-accuracy contracts.

The transaction retained a byte-exact private configuration backup, moved the
preceding package to an identity-qualified rollback boundary, activated the
verified stage and performed one targeted Module Control reload. No property
was written and no `ApplyChanges()` configuration transaction was performed.
Automatic rollback was armed but not required.

The active package identity is
`15bb10a7f938753005e6ba63ea1c81c3f03a2e5af16e435c06a4aa7381948b4d`.
The immediately preceding identity
`6cfc1cba63add51505a460f139ea5c547dd0ccd88c7ac1a2e18c6a1cac800bb0`
remains the complete rollback package.

## Independent Postflight

A separate read-only probe verified:

- healthy status `102` and exact active and rollback identities;
- byte-identical complete configuration, provider policy and WebHook Control;
- three source contracts with type triples `[3, 1, 1]` and all nine required
  source variables still logged;
- two unchanged visualization links;
- no target stage or failed-candidate residue;
- retained private upload and byte-exact configuration backup; and
- an intact provider cache with 66 regular files, 2,214,383 bytes and no
  symbolic links.

A separate deployed-code archive probe projected one representative day before
August for all three sources. It returned non-zero paths for every source and
marked all rendered samples as unverified because historical accuracy evidence
was absent. The probe disclosed neither coordinates nor movement records and
did not write archives or instance state.

## Privacy And Remaining Acceptance

No ObjectID, source key, tracker identifier, coordinate, tile index, movement
history, private path, Connect origin or host metadata is recorded here.
Activation and postflight did not open the live map and therefore caused no
deliberate provider request.

Physical acceptance should repeat one previously incomplete fitted path and one
pre-August path. The first must fill without abruptly terminated tile strips;
the second must render with the explicit unknown-accuracy indication. A live
browser check may transmit location-derived tile indices to the configured OSM
provider and therefore remains a distinct action-time privacy confirmation.
Commit, publication, cache purge and retained-artifact cleanup remain closed.
