# Host Controls And Theme Live Activation

**Status:** Live package, independent structural postflight and physical
Safari/Mac and iPad acceptance passed

**Date:** 2026-08-31

## Authorized Change

The separately authorized package-only transaction activated the exact 29-file
candidate:

```text
c6b93f60167a18ec11f1eba39d11fb36c7fdda116555ff8d594e3485b7386864
```

No instance property, provider policy, persistent WebHook, visualization link,
OwnTracks object, logging state or archive setting was written. The targeted
Module Control reload was the only runtime action after the atomic package
swap.

## Transfer And Rollback Boundary

The local archive passed the complete OwnTracks test target and deterministic
fileset check before transfer. Windows received it in bounded chunks, verified
the archive size and SHA-256, rejected unsafe or duplicate paths, extracted
every file through exclusive streams and rechecked all payloads against the
package manifest.

The immediately preceding active package remains available as a complete
29-file rollback. The successful transaction left no staging or failed-package
directory. The verified transfer archive remains retained because cleanup was
not part of this gate.

## Independent Postflight

A separate read-only postflight proved:

- the intended candidate is active and the pilot remains healthy;
- the preceding package is intact at the new rollback boundary;
- pilot and provider configuration are byte-identical to their preflight
  baselines;
- source order remains `LT`, `CT`, `MT` and the one-hour maximum path gap is
  unchanged;
- all three source-instance configurations, variable types and logging states
  survived the reload unchanged;
- both additive links and persistent WebHook Control remain unchanged;
- the same-origin tile capability policy remains active while routing remains
  disabled; and
- the installed renderer contains the reserved host band, native map-root
  gesture boundary, viewport-only OpenLayers gesture ownership, equal field
  grid and Symcon theme-variable integration, with no backdrop filter.

No ObjectID, tracker identity, coordinate, private path or movement history is
recorded in this artifact.

## Physical Acceptance

The subsequent physical Safari/Mac and iPad acceptance verified:

1. Source, Day and Path respond immediately and use the expected pointer;
2. `LT` is the default;
3. `CT` fit-all contains only the selected `CT` track;
4. the external path appears only after explicitly selecting it and no external
   point leaks into an OwnTracks source;
5. all three fields have calm, equal geometry without an oversized Day field;
   and
6. controls and map follow the active Symcon dark skin.

All six checks passed. The next repository-only presentation refinement is
recorded in `41-current-overview-and-direction-arrows.md`. Any further live
package mutation, rollback, cleanup, commit or publication remains a separate
gate.
