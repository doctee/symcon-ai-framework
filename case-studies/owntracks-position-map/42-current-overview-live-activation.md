# Current Overview Live Activation

**Status:** Exact-package activation and structural postflight complete;
physical iPad acceptance complete with bounded follow-up findings

**Date:** 2026-08-31

## Authorized Scope

This gate activated the previously verified deterministic package and changed
only the three private source display labels in their existing source order.
It did not authorize a commit, publication, provider change, retained-artifact
cleanup or mutation of an existing OwnTracks object, Archive Control, logging
or visualization link.

The private labels, object identifiers, tracker identifiers, coordinates and
movement history are deliberately absent from this record.

## Guarded Activation

Immediately before the first mutation, the transaction revalidated the active
and staged package identities, exact previous configuration, provider policy,
source order, pilot status, upload digest and free transaction paths. It then:

1. wrote and rehashed an exclusive private backup of the complete previous
   configuration;
2. moved the active package to an identity-qualified rollback directory;
3. activated and reloaded the exact staged package;
4. verified that the reload had not changed the configuration;
5. applied only the three private display labels; and
6. retained an automatic failure path capable of restoring both the previous
   package and complete previous configuration.

## Independent Postflight

A separate read-only postflight verified:

- the active identity and every file in the 29-file package;
- active pilot status;
- exactly three current-position contracts in the unchanged source order;
- a complete configuration comparison whose only differences are the three
  private labels;
- unchanged provider configuration and one-hour maximum line gap;
- unchanged persistent WebHook configuration;
- the unchanged number of visualization links;
- the exact 29-file rollback package and exact previous configuration backup;
- retention of the byte-verified upload; and
- no staging or failed-package residue.

No archive history was read during this activation or postflight. No source
payload, coordinate, ObjectID or movement history was emitted into repository
evidence.

## Remaining Acceptance And Gates

Physical iPad acceptance confirmed the activated package is usable and exposed
the bounded presentation, ETA, arrow and regional-tile follow-ups recorded in
`43-overview-controls-eta-and-tile-coverage.md`. Safari/Mac acceptance of the
next correction remains pending. The acceptance scope includes:

- the default three-source current-position overview with the private labels
  and timestamps;
- absence of the former external-path browser option;
- source-local day history after selecting `Path`;
- readable, movement-oriented direction arrows; and
- compact, non-overlapping controls with working fit, zoom and pan.

Commit, publication and removal of the retained upload, rollback package or
configuration backup each remain outside this gate.
