# Viewport Retry Live Activation

**Status:** Exact package-only activation and independent structural postflight
complete; physical tile acceptance pending

**Date:** 2026-09-02

## Approved Scope

The gate authorized repository implementation, tests, private live activation
and browser verification. It did not authorize a configuration change,
repository commit, publication, cache purge, retained-artifact cleanup,
OwnTracks-object change, archive/logging change, visualization-link change,
WebHook change or provider-budget change.

## Transfer And Activation

A fresh Symcon-MCP-only preflight verified one healthy owner, the exact active
package and complete configuration, unchanged protected tile and provider
boundaries, and collision-free transaction paths. The 199,273-byte archive was
transferred in eight bounded chunks. Windows verified its archive digest,
exactly 36 safe regular entries, all 34 provenance payloads, every payload size
and digest, the fileset identity and the fresh-viewport retry contract.

The transaction retained a byte-exact private configuration backup, moved the
preceding package to an identity-qualified rollback boundary, activated the
verified stage and performed one targeted Module Control reload. No property
was written and no configuration `ApplyChanges()` transaction was performed.
Automatic rollback was armed but not required.

The active identity is
`44ded6323a8030af194af22df66f0e94cf74bfdd59b9aa1ee006b0221850d402`.
The immediately preceding identity
`a03d09d1cf7cb66544bc125e679d4fec71412dfc5ff96ef591d26301efc08bc6`
remains the complete rollback package.

## Independent Postflight

A separate read-only probe verified:

- healthy status `102` and exact active and rollback identities;
- all 36 regular files and all 34 payload hashes in both packages;
- byte-identical complete configuration, protected WebHook mode and provider
  fallback mode;
- the fresh-viewport renderer contract in the installed bundle;
- no symbolic link, target-stage or failed-candidate residue; and
- retained private upload, byte-exact configuration backup and intact provider
  cache.

The synthetic browser gate had already rejected the complete first viewport
generation and observed automatic generation-two recovery with every visible
tile loaded. The subsequent in-app live navigation was denied before the page
opened because its security review requires an additional action-time
confirmation for transmitting position-derived tile indices to the configured
OSM provider. No browser workaround was attempted. Physical live acceptance
therefore remains pending and this is not evidence of a renderer failure.

## Privacy And Remaining Boundaries

No ObjectID, source key, tracker identifier, coordinate, tile index, movement
history, private path, Connect origin or host metadata is recorded here. The
verified upload, configuration backup and rollback package remain retained.
Commit, publication, cache purge and retained-artifact cleanup remain closed.
