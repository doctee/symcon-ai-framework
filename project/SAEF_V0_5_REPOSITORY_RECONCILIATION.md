# SAEF v0.5 Repository Reconciliation

**Status:** Repository inventory reconciled; scope-freeze gate open
**Date:** 2026-09-12
**Published baseline:** `v0.4.0`
**Reconciled revision:** `d278226bf8f038e501ba1479b0bc2ea2ab867244`

## Purpose

This document reconciles the repository after the initial v0.5 inventory had
been overtaken by implementation, Windows qualification and independently
gated live case-study work. It records what belongs to the public development
line without importing private installation identities or converting previous
authorization into future authority.

## Repository State

At reconciliation start:

- the primary checkout was clean and exactly aligned with `origin/main`;
- pull requests 74 through 119 had been integrated after `v0.4.0`;
- the latest repository and pull-request CI was green;
- no pull request was open;
- GitHub issue 1 remained open; and
- the public helper contract still contained 30 functions and three constants.

The post-v0.4 changes fall into four public groups:

1. exporter-private MQTT latest-command-wins behavior;
2. repository and Composer workstream isolation;
3. restricted Channel-v8 standalone deployment, scope-bound approval and
   secure child-process execution; and
4. OwnTracks, Navimow and Open-Meteo case-study evolution that exercises those
   contracts without extending the public helper API.

## Reconciled Operational Evidence

### Deployment channel and approval

The current Channel-v8 generation passed Windows PowerShell 5.1 parser, ACL,
negative-boundary and secure-child-process qualification. Protected installation
and independent postflight completed with the same five remote verbs.

One hash-pinned OwnTracks profile was installed through a separate gate. A
fresh server plan then completed one scope-bound activation and independent
channel and Symcon postflight. That result proves the reference composition
only. It does not authorize MediaCarousel, another plan or retention deletion.

### Navimow

The repository and standalone-module history now includes mowing analytics,
an HTML SDK local map and the subsequent Safari, metadata and presentation
corrections. The fixed-size legend revision completed canonical publication,
controlled Symcon update, immediate and delayed postflight, live tile inspection
and physical iPad validation. The rollout is complete; only its separately
authorized cleanup classification remains.

### MQTT latest-command-wins

The repository implementation and deterministic tests are integrated. The
private live-migration directory contains planning and rollback inputs but no
canonical completion record. Issue closure and any live-completion statement
therefore require a fresh bounded owner and consumer check.

## Drift Corrected by This Reconciliation

The previous current-state documentation still described:

- latest-command-wins as awaiting repository integration;
- an empty standalone-module target allowlist;
- secure child-process and approval installation as closed future gates; and
- OwnTracks one-click activation as not yet performed.

Those statements were historically correct but no longer described current
`main`. The v0.5 inventory, project overview and contract-status sections now
separate completed reference evidence from still-closed future authority.

The reconciliation also recovered the generic template and case-study concept
documents from an unintegrated historical branch. Current Navimow documents
already linked to those paths, so restoring and updating them removes broken
framework references without importing obsolete module code.

## Compatibility Assessment

- No public helper function or constant changes after v0.4 are admitted.
- Channel version remains 8 and retains `probe`, `stage`, `preflight`,
  `activate` and `status`.
- Existing runtime-fileset deployment remains supported.
- Standalone targets still opt in through protected local profiles.
- One-click approval does not absorb publication, restart, provider or cleanup
  authority.
- Historical manual deployment and recovery workflows remain valid for their
  exact source generations.

The proposed changes are additive and suitable for a minor release after exact
version, generated-artifact and release-readiness checks.

## Open Repository Work

1. Reconcile and, after fresh live evidence, close or revise GitHub issue 1.
2. Extract the uncommitted MediaCarousel adapter from its historical worktree;
   do not build or publish from that recovery checkout.
3. Implement cross-root standalone retention only in its own deletion-authority
   workstream.
4. Classify historical worktrees and branches before any removal.
5. Freeze v0.5 scope and prepare exact version and release-readiness changes.

## Decision

The repository is coherent enough to proceed to v0.5 scope freeze. It is not
yet release-ready, and this reconciliation grants no publication, live,
restart, provider, retention or cleanup authority.
